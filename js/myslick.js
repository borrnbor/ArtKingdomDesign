$(function() {
  $('.projPopup__slider').slick({
    infinite: true,
    // autoplay: true,
    autoplaySpeed: 2000,
    speed:1500,
  })
})

const myCarousel = new Carousel(document.querySelector("#myCarousel"), {
  // Your options go here
});

